<configuration>
  <!-- Appender untuk console -->
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Appender untuk Datadog -->
  <appender name="DATADOG" class="com.datadoghq.logback.DatadogLogbackAppender">
    <httpApiKey>03e9b0afd20dba2ce4874488eaec44ec</httpApiKey>
    <service>product-management</service>
    <host>node2</host>
    <source>java</source>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Root logger -->
  <root level="info">
    <appender-ref ref="STDOUT" />
    <appender-ref ref="DATADOG" />
  </root>
</configuration>